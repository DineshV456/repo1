name: Hello Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Say Hello
        run: echo "👋 Hello from GitHub Actions!"

      - name: Step 2 - Print the date
        run: date
